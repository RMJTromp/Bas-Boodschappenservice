div.modal {
    position: fixed;
    inset: 0;
    background: #00000061;
    display: none;

    > div.card {
        min-width: 300px;
        width: fit-content;
        padding: 30px 20px;

        > header {
            margin-bottom: 20px;
        }

        > footer {
            margin-top: 20px;
            display: flex;
            justify-content: end;
            gap: 1rem;
        }
    }

    &[open] {
        display: grid;
        place-items: center;
    }
}